# 观澜重构 V1 · 2026-08-19

> 起因：「构建的思路还是不清晰，有点乱。特别是搜索这一部分，同时保种、辅种、转种都还有很大的问题。」
> 这一版做的是**内部分层**：单文件不变，部署链路不变，但底下的骨架换了。

---

## 一、病根：没有「一份内容」这个东西

同一份数据过去有**五本账**，各记各的，靠裁字符串互相猜：

| 账本 | 主键 | 名字从哪来 |
|---|---|---|
| `torrents` | info_hash | tr 里的种子名 |
| `media` | info_hash | qb 里的种子名 |
| `keepseed` | 自增 id | **站点标题**，连扩展名都对不上 |
| `matches` | 自增 id | 别站上的名字 |
| qb 的 tag / tr 的目录 | 无 | 隐式约定 |

于是代码里出现过 `base = name.rsplit(".",1)[0] if "." in name[-6:]` 这种东西——
用字符串裁剪去猜「站点标题」和「种子名」是不是同一个东西。这类猜测每多一处，系统就更乱一分。

### 治法：`content_id` —— 把算完就扔的东西留下来

`run_match` 里本来就在算文件清单（`local_set`），**算完就扔**。它就是身份本身：

> 同一份数据在不同站 info_hash 不同、名字不同，但**相对路径 + 文件大小完全一样**——
> 这正是辅种能成立的根据。

```python
def content_id(manifest):
    items = sorted((str(p), int(sz)) for p, sz in dict(manifest).items())
    return hashlib.sha1("\n".join("%s\t%d" % (p, sz) for p, sz in items).encode()).hexdigest()[:16]
```

三个来源（tr / qb / .torrent 文件）算出的必须是同一个值，口径差一点整套账就废了——
`tests/test_ledger.py` 里有 4 个证伪用例专门盯这条。

---

## 二、三个正交维度，过去挤在一个 `status` 字段里

| 维度 | 取值 | 记在哪 |
|---|---|---|
| **role** 角色 | `library` 媒体库资产 / `stock` 保种库存 | `content.role` |
| **place** 位置 | `qb` / `tr` / `gone` | `content.place` |
| **coverage** 辅种覆盖 | 每个站单独一行 | `coverage` 表 |

正交的东西塞进一个字段，就会出现「保种种子交棒到 tr 之后 status 该写什么」这种答不上来的问题。

---

## 三、四块各改了什么

### 🌱 辅种：从「辅到一个就收工」改成「消 pending」

老代码里这一行，跟「辅种尽可能囊括所有站」是**正面冲突**的：

```python
if matched > 0: break   # 这个关键词辅到了，不必再试下一个
```

新的 `crossseed_one()` 三处关键区别：

1. **只问账上还欠的站**（`led_cov_pending`），不是每次全站重来
2. **不再 break** —— 每个站独立记账，认领与否互不影响
3. **逐站落账**，而且严格区分：

| 状态 | 含义 | 老代码 |
|---|---|---|
| `source` | 本来就是从这站下的 | 混在一起 |
| `seeding` | 已在这站做种 | 混在一起 |
| `absent` | **问过了，这站确实没有** | ← 这两个 |
| `error` | **没问成**（cookie 过期/超时） | ← 分不开 |
| `banned` | 该站 ban 了 tr 客户端，永远别问 | 每次都问一遍 |

`absent` 带时间戳，30 天后自动转回待问；`error` 下一轮必重试。
**「辅种辅不全」从此是可见的**——缺种报告直接读账，不再是反推的猜测。

### 顺带治好的一个真 bug：站名对不上

tracker 域名推出来的是 `ttg`，Prowlarr 里叫 `TTG`，**大小写不同就被当成两个站**——
自己正在做种的来源站，每一轮都被当成「从没问过」重问一遍。
老代码在 `gap_report` 里用双向子串兜过这个坑，但没根治，所以文案只能写「搜不到≠一定没有」。

现在用 `match_site()` 统一：精确匹配 → 按 Prowlarr 自己的 `indexerUrls` 反查注册域 → 双向子串兜底。
站名不再靠手抄表 `SITE_MAP` 猜。

### 🚚 转种：先把三个「转」分开

**观澜里有三件完全不同的事都被叫过「转种」**，这是概念混乱最大的一处来源：

| 叫法 | 是什么 | 受禁转红线约束吗 |
|---|---|---|
| **交棒** `transfer_to_tr` | qb 下完 → tr 接手做种。同一份数据换客户端，**站点不变** | ❌ 压根没换站 |
| **转发种** `xfer_pack` | 把 A 站资源发布到 B 站。**这才是 PT 圈说的转种** | ✅ 硬拦截 |
| **辅种** `crossseed_one` | 别站已有种子，把 tracker 挂上去一起做种 | ❌ 不产生新发布 |

三者风险等级完全不同，合在一个词里迟早出事。界面 Tab 也从「保种转种」改成「做种运营」。

### 交棒失败终于有人管了

老代码的病灶在 `qb_watcher`：

```python
row = SELECT status FROM media WHERE info_hash=?
if row: continue     # ← 入库过一次(哪怕是 hold)，交棒失败就永远不会再试
```

入库和交棒是**两件正交的事**，却被这一个判断绑死了。现在：

- 入库看 `media` 表，交棒看 `instance` 表（tr 那边接手了没有）——**从事实推导，不靠队列表**
- 交棒失败记 `content.xfer_fail`，下一轮自动重试，连败 5 次才收手
- 面板能看见卡住的，可人工重置

### 🌊 保种：灌进来的东西，终于有出口了

「到期删目录即清仓」这句话过去**只存在于文案里**，代码里没有出口。
更糟的是 `health_report` 故意排除了保种目录（注释：「保种目录的冷种是本意」），
于是全站最该被审视的那批种子恰恰没人看。

新增 `stock_report()` 库存台账，按**证据硬度**分三档：

| 档 | 依据 | 建议 |
|---|---|---|
| 🔴 站点已删种 | tracker 报 `unregistered` | 硬证据，清了不影响考核 |
| 🟠 tracker 连不上 | announce 持续失败 | 也可能是站点抽风，看一眼再说 |
| ⚪ 大体积零上传 | 30 天以上 + ≥20GB + 零上传 | **只陈列，不建议清**——保种本来就是备着 |

`stock_evict()` 的护栏写死在函数里，不给绕过的口子：只清数据目录在 `KEEP_DIR` **之下**的。

### 顺带修掉一个能删错数据的隐患

原先 4 处路径判断用的是裸 `startswith`：

```python
if dd.startswith(keep)   # keep="/data/downloads/keepseed"
```

`/data/downloads/keepseed-old` 也会通过。用在清退护栏上，就是**把不该删的目录当成保种库存删掉**。
统一换成 `_under()`，卡住目录边界。

另外 `_canceldl`（面板「取消下载」）原先对**任意 hash** 直接删任务+删数据，没有任何限制——
传进来一个已完成的，做种立刻断。现在只允许取消 `progress < 1` 的。

### 🔍 搜索：别名扇出的死结解开了

搜索体系文档里，「别名扇出」这一项一直标着 **「A 别动」**，原因是实测：
三个词扇出去墙上时间 +28%，只多认领 0~5 条。

根因不是扇出本身，是**挑出来的三个词是同一个入口的三种拼写**——
《白色巨塔》挑出来的是 `Shiroi Kyotou` / `Shiroi kyotô` / `Shiroi Kyoto`。

`alias_plan()` 的做法：按文字分桶（汉字 / 假名 / 谚文 / 拉丁）→ 桶内按拼写相似度聚类 →
**各桶之间轮转取**。挑出来的才是不同的召回入口：

```
白色巨塔 → ['白い巨塔', 'Shiroi Kyoto']      ← 日文原名 + 罗马字，两个入口
只有三个罗马字变体时 → ['Shiroi Kyoto']      ← 不硬凑，硬凑就是白花一波
请回答1988 → ['응답하라 1988', 'Reply 1988']  ← 谚文 + 英文
```

### 四条线各自的策略，从代码里提出来变成数据

```python
POLICY = {
    "find":      {"timeout": 9,  "deadline": 22,  "workers": 96, "intent": "browse"},
    "crossseed": {"timeout": 20, "deadline": 300, "workers": 8,  "intent": ""},
    "harvest":   {"timeout": 9,  "deadline": 25,  "workers": 32, "intent": "collect"},
    "stock":     {"timeout": 30, "deadline": 0,   "workers": 1,  "intent": ""},
}
```

加一个新场景 = 加一行策略，不再是「在三个地方各塞一个 if」。

---

## 四、调度也顺手改了

老的 `scanner()` 每 30 分钟把 tr 里 4000+ 个种子**连完整文件列表一起**拉一遍（几十 MB JSON），
然后靠「6 小时冷却」拍脑袋跳过。新版：

1. 先用轻字段拉列表，**只对选中要辅的那几个**再拉文件清单
2. 优先级按「账上还欠几个站」排，没登记过的新内容最优先
3. 有预算（`CROSSSEED_BUDGET`，默认 15/轮）——4000 份内容不设预算就是自己 DDoS 自己
4. **是收敛的**：全覆盖的内容自动退出队列，不像老版每轮全量重来

---

## 五、怎么验证

```bash
python3 check.py
```

一条命令跑完 4 条铁律 + 7 组测试（82 项）：

| 检查 | 查什么 |
|---|---|
| 唯一 SQL 出口 | 总账三张表只能经 `_led()`，别处不许直连 |
| 删除护栏 | 删数据的函数必须走 `_under()`，或在白名单里写明护栏形式 |
| 模板转义 | JS 里不许有反斜杠转义（求值后变裸引号 → 整页白屏） |
| 分区标记 | § 分区注释还在 |
| 7 组测试 | 账本 / 辅种 / 交棒 / 保种 / 别名 / 路由 / 页面 JS |

**这些不是摆设**，写的过程中它们真抓到了 4 个问题：

1. `led_get()` 漏 SELECT 新加的列 → 交棒失败上限那道闸门完全失效（不报错、日志正常）
2. `_canceldl` 对任意 hash 删数据
3. 4 处裸 `startswith` 的目录边界漏洞
4. 路由里 `_send_json` 写成 `_json` → 一上真实例就 500（单元测试碰不到 HTTP 层）

其中第 4 个只有**真起服务**才能发现，第 1、3 个只有**真跑一遍**才能发现。

---

## 六、部署

代码只改了 `packseed.py` 一个文件，部署链路完全不变：

```bash
scp packseed.py 群晖:/volume7/docker/compose/packseed/packseed.py
```

**推之前先比对 md5，确认 NAS 上没人直接改过**；推之后备份一份 `.bak-20260819`。
然后重启容器（这条我跑不了，`docker` 在群晖上要 sudo 密码）：

```bash
sudo docker restart packseed
```

### 升级后第一次启动会做什么

- 三张新表自动建（`CREATE TABLE IF NOT EXISTS`），老表加 `cid` 等列（`ALTER TABLE`），**老数据一条不动**
- 检测到总账为空 → 后台跑一次**回填**：把 tr 里现有种子全部登记进账，
  **只算身份，不发一个搜索请求**。4000 个种子分批拉，几分钟的事
- 回填完，缺种报告立刻有内容；coverage 从零开始按轮次填

没回填的话，账要靠每轮 15 个慢慢啃，一周才认全，期间面板基本是空的——会让人以为功能坏了。

---

## 七、还没做的

| 项 | 为什么先不做 |
|---|---|
| 物理上把代码按 § 顺序重排 | 收益递减。分区注释 + `check.py` 已经能约束调用方向，大规模挪动代码风险高于收益 |
| 站点权重加类型维度（搜索体系文档的 C 项） | 要改 `ixstat` 表结构 + 迁移，独立一件事 |
| 质量档位选择（D 项） | 打分层已统一，加参数即可，但要动前端 |
| 「已入库」下沉到种子行（E 项） | 小改进，排在后面 |
| 8 个 Tab 的整体重组 | 这轮只动了「保种转种」→「做种运营」。整体重排是另一个话题，得先想清楚信息架构 |
| `gapV()` 前端死代码 | 发种查重的后端还在（`gap_verify`，宽松口径，与辅种故意不同），但前端按钮已被「⚡ 补问」取代。等发种流程整体理一遍时一并处理 |
