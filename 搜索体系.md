# 观澜搜索体系

> 2026-08-03 整理，2026-08-05 更新。把零散提的搜索需求收成一个体系、找出规律、列出改进项。
> 状态：统一内核 + B（跨站去重）+ I（中文音轨优先）**已实施**；A/C/D/E/F 待做。

---

## 一、需求回顾：你提的话，和它背后真正在说什么

| 你的原话 | 本质上在要求什么 | 属于哪一层 |
|---|---|---|
| 搜索要等 40 多秒 | 响应时间要有**上限**，不能被最慢的站拖死 | 编排 |
| 加了年份还是搜不准，"真正要的剧出现在其他里面" | **召回**要能连上拼音/罗马字命名的种子；**排序**要把要找的那部顶到最前 | 查询理解 |
| 刷新一下搜到的东西又没了 | 搜索结果是**有成本的资产**，不能随手丢 | 呈现 |
| 先点电影/电视剧再输名称 | 用**意图**收窄搜索面 | 编排 |
| 大站资源多，优先他们，小站不必紧跟着耗 | 选站要按**历史产出**加权，不是一视同仁 | 编排 |
| 搜索少而精，辅种尽可能囊括所有站 | **同一个搜索动作，用途不同，策略必须不同** | 编排 |
| 音乐按年代排、按经典程度排名、推荐下载 | 排序的依据不是"做种多"，而是**这个用途下什么叫好** | 排序 |
| 绝命毒师按季排、按制作组排、尽可能统一、合集也显示 | 结果要**结构化**呈现，而不是一坨列表 | 呈现 |
| 乱七八糟的东西只要不影响整体就别乱改 | 自动化要**知道自己什么时候不该动手** | 全局 |

一句话概括：**从"把种子列出来"，变成"帮我做决定"。**

---

## 二、从踩过的坑里总结的规律

这七条是这段时间每个 bug 的根因，不是空泛的原则。

### 1. 锚定优于解析
先确定"你要找的是哪部作品"（TMDB 锚点），再让种子来认领；
不要反过来从种子名去猜作品。

**为什么**：PT 发布组给中文剧起名用拼音（`Da Ming Wang Chao`）、
日剧用罗马字（`Shiroi Kyotou`）、韩剧用英文（`Reply 1988`）。
靠解析种子名分组，永远连不上《大明王朝》和 `Da Ming Wang Chao`。

### 2. 模糊匹配只在信息量足够时开放
中文可以按字符重合（繁简大多只差个别字）；
拉丁字母**绝对不行** —— 26 个字母在任何标题里都反复出现，重合率没有区分力。

**教训**：`For The Children` 的字母集能在
`齐秦-丝路1996-FLAC分轨-Chris@OpenCD` 里凑齐 11/13，纯噪音也能过线。

### 3. 比较之前先剥噪声
`- Single`、`(Remastered)`、繁简、全半角标点，
不剥掉就只能靠模糊蒙，一蒙就错配。

**但不能剥过头**：`(Live)` 不能剥 —— 那是另一张作品，剥了会让 Live 版去抢正传的种子。

### 4. 一个资源只能被认领一次
去重要**真的执行**。写了 `used` 集合却没在筛选时用，是死代码，
表现出来就是同一个种子在界面上出现两行。

### 5. 判不出就不动手，而且要说出来
猜季号会把三季文件塞进一季；猜制作组会冒出一堆叫"1080p"的假组。
判不出就留空、留原地、单列出来交给人 —— 但**必须让人看见**，
不能默默跳过。（媒体库体检的"待人工确认"就是这条的产物。）

### 6. 同一部作品的各个部分要统一
多季剧的制作组要统一，否则 Emby 里第 1 季 4K Netflix、第 3 季 1080p 蓝光，很难看。
推荐依据是**覆盖季数**，不是做种数 —— 做种最多的往往是跨季合集，帮不上按季挑的忙。

### 7. 接口全绿说明不了什么
横滚容器里的 `loading="lazy"` 永远不触发、`img` 给 `display:flex` 会渲染空白、
JS 里混进全角括号 Python 的 `ast.parse` 查不出来。
**验证路径必须能证伪**：真浏览器截图、`node --check`、比对进程启动时间与文件写入时间。

---

## 三、现状诊断：同一件事有三份实现

这是"补丁摞补丁"的**具体形态**。不是某个 bug，是结构问题。

### 三套「解析种子名」

| 在哪 | 怎么解析 |
|---|---|
| `search_group` | `_season_of` / `_relgrp` / `_is_pack`（新加的，比较完整） |
| `_pick_release` | 内联 `"x265" in t`、`"1080p" in t`，字符串裸判，没有抽象 |
| 音乐那套 | `_LOSS_RE` / `_SPLIT_RE` / `_WHOLE_RE` / `_SINGLE_RE` / `_NONSTUDIO` |

### 三套「给种子打分」

| 用途 | 依据 |
|---|---|
| 交互搜索 | 纯 `seeders` 排序 |
| 榜单批量 `_pick_release` | 体积窗口 + seeders + x265(+30) + 分辨率偏好 |
| 音乐 `_score_music` | seeders + 分轨(+500) + FLAC(+120) + 精选(-250) + 单曲(-600) |

同一个「哪个版本更好」，三个互不相同的答案。
**加一个需求要改三处，改漏一处就出怪事。**

### 三套「选哪些站」

| 用途 | 怎么选 |
|---|---|
| 交互搜索 | `ix_weights()` 按历史产出**学**出来 + `MAJOR_SITES` 点名 + 类型粗筛 |
| 音乐 | 硬编码常量 `MUSIC_SITES` |
| 榜单批量 | 硬编码常量 `BATCH_SITES` |

根因：`ixstat` 表只有 `(站名, 次数, 产出)`，**没有类型维度**，
学不出"这个站出剧多但没音乐"，所以另外两处只能写死。

---

## 四、目标架构：五层，每层只干一件事

```
L0  查询理解    split_query → query_anchor
                "白色巨塔 2003" → {作品=白色巨塔, 年份=2003, 别名=[Shiroi Kyotou, ...]}
                ↓
L1  站点编排    按用途选站 + 并发扇出 + 加权收网
                交互=少而精有截止 / 辅种=全站等到底 / 批量=几个大站
                ↓
L2  解析        parse_release(种子) → {季号,合集,制作组,分辨率,片源,编码,无损,分轨,…}
                【唯一解析入口】读不出来就留空，不猜
                ↓
L3  排序        score_release(种子, intent) → 分数
                【唯一打分入口】intent = browse / collect / music
                ↓
L4  呈现        分组 → 去重 → 推荐 → 落盘
                按季分段、跨站合并、推荐统一制作组、结果存 localStorage
```

**判断一个新需求该改哪里**：
- "搜不到" → L0（召回）
- "太慢" → L1（编排）
- "认错了/没认出来" → L2（解析）
- "顺序不对" → L3（打分）
- "看不清楚" → L4（呈现）

一个需求只应该落在一层。落到两层以上，说明分层没切对。

---

## 五、统一改造（✅ 2026-08-05 已实施）

1. **`parse_release(it)`** —— 全站唯一解析入口。
   输出：`ss / pack / grp / res / src / codec / hdr / lossless / split / single / nonstudio`。
2. **`score_release(r, intent)`** —— 全站唯一打分入口。三种 intent：
   - `browse`：做种数为主，画质只在 ±3 分内微调（**不替人做主**，他可能就想要 4K 原盘）
   - `collect`：体积窗口 + x265 + 分辨率偏好（无人值守，必须自己拿主意）
   - `music`：分轨/正传/无损是硬指标（沿用现有权重，行为不变）
3. **`dedupe_releases(rs)`** —— 新能力。同一发布多站都有的合成一行，
   其余站收进 `alts`（不丢数据）。实测绝命毒师第 1 季 11 条里 3 条是同一个 HHWEB 发布。
4. 搜索结果表加**画质列**、跨站合并显示 `+N 站`。

`_pick_release` / `_score_music` 的调用方一行不用改，行为等价。

---

## 六、还能再优化的地方（待讨论）

按"值不值得做"排序。**我的建议：做 A、B、C、D，E/F 顺手，G/H 先不做。**

### A. 别名扇出 —— 召回率最大的缺口 ⭐ 强烈建议
现状：`query_anchor` 已经从 TMDB 拿到了 `alternative_titles`，但只挑**一个** `altq` 去搜。
提议：并发发 2~3 个最可能命中的别名（中文名 + 英文名 + 罗马字/拼音）。

- **收益**：直接解决"真正要的剧搜不到"。这是规律 1 的完整落地，现在只做了一半。
- **成本**：中。查询数翻 2~3 倍，但 `prowlarr_search_fan` 本来就是并发的，
  加权收网也已经有了，墙上时间基本不变。
- **风险**：别名太宽会引入噪声（比如英文通用词），需要用锚点回收 —— 这套机制已经有了。

### B. 跨站去重 ✅ 已实施
- **实测**：绝命毒师 98 → 23 行（合并 75）、肖申克一条合并 31 个站。
  抽验确认合并的是同一个 FRDS 发布在不同站，不是合错。
- 抠不出制作组的不合并；`alts` 保留每个站的完整信息，不丢数据。

### C. 站点权重加类型维度 ⭐ 建议
现状：`ixstat(name, n, res)` 是全局的。
提议：改成 `ixstat(name, cat, n, res)`，按 (站点 × 类型) 学。

- **收益**：`MUSIC_SITES` / `BATCH_SITES` 两个硬编码常量可以**删掉**，
  变成自动学出来的。这是"少而精"真正的自动化。
- **成本**：中。要改表结构 + 迁移（老数据可以当 `cat=''` 保留）。
- **风险**：冷启动阶段没数据，需要回退到全局权重。

### D. 质量档位 ⭐ 建议
提议：搜索框旁边加一个档位选择（4K / 1080p / 不限），落到 `score_release` 的参数。

- **收益**：配合"尽可能统一" —— 挑完制作组还能挑画质档，多季剧才真正整齐。
- **成本**：低（打分层已经统一，加一个参数的事）。

### E. 「已入库」下沉到种子行
现状：`owned_label` 只标在作品卡片上。
提议：标到种子行 —— "你已有这个组的第 1、2 季"。

- **收益**：避免重复下载，也提示该跟哪个组。**成本**：低。

### F. 搜索结果短 TTL 缓存
同一个词 5 分钟内重复搜，直接返回上次结果。

- **收益**：小（前端已经存 localStorage 了，只在换设备/换标签页时有用）。
- **成本**：低。顺手做。

### G. 多锚点 —— 暂不建议
搜"三体"会同时有动画/剧集/电影。现在只锚一部。
- 只在少数歧义词上有用，而这些词现在也能靠卡片墙看到，收益不明显。

### H. 站点健康度面板 —— 暂不建议
把 `ixstat` 可视化出来（哪个站常超时、哪个站 cookie 过期）。
- 现在靠搜索日志那几行也够用；等 C 做完、表里有了类型维度再做更值。

### I. 中文音轨优先 ✅ 已实施（2026-08-05 新增需求）

起因：用户发现很多剧没有中文音频。全库 108 部普查：有中文 44、**只有外语 38**、未标语言 23。

- **L2 解析**新增 `zhkind` / `zhrank`：双语(3) > 国语(2) > 粤语/多音轨?(1) > 无(0)
- **L3 打分**：只有 `collect`（批量下载、无人值守）加分（双语+250/国语+200/存疑+60）；
  `browse` **不加分** —— 人在屏幕前，把 500 做种的原盘挤到国语版后面是替人做主
- **L4 呈现**：排前面 + 绿色角标 + 「只看中文音轨」开关；一个都没有时明说
  "多半是这部本来就没国配，不是没搜到"

两个关键判断：
1. **字幕 ≠ 音轨**。「中英双语字幕」「中字」「简繁」说的是字幕，认成国语会让人白高兴。
   `双语` 后面跟 `字幕` 的一律排除。11 条构造用例 + 真实种子名双重验证。
2. **`4Audio` 只能标"存疑"**。中文站上 3 条以上音轨基本含国语（实测 Zootopia 那个
   4Audio 的碟确实带国语），但种子名没明写 —— 按规律 5，推断出来的东西要
   单开一档、让人看见，不能冒充明写的。

> ⚠️ **不能给已下载的文件补音轨标签**：媒体库 105/108 部是硬链接，
> `mkvpropedit` 改文件头 = 做种那份同时被改 = 掉种。这条路封死。

---

## 七、需要你拍板的

1. ~~A~F 做哪些~~ → 已定：A+B+C+D，E/F 带上。**B、I 已完成，剩 A、C、D、E、F。**
2. C 改表结构 → 已定默认：老数据**保留为 `cat=''`**，正好当冷启动回退。
3. D 的档位默认值 → 已定默认：**不限**（browse 意图的原则就是不替人做主）。

### A 的成本实测（2026-08-05，做之前先量了）

| | 耗时 | 认出是这部的 |
|---|---|---|
| 只搜原词「白色巨塔」 | 9.6 秒 | 127 条 |
| 三个词一起扇出 | 12.3 秒 | 136 条 |

**两条旧注释都被证伪了** —— 既不是「只花一波的时间」，也不是「多花几秒」，是 **+28%**。

更值钱的发现：**现在的别名池全是近似重复**。挑出来的是 `Shiroi Kyotou` /
`Shiroi kyotô` / `Shiroi Kyoto` —— 同一种命名法的三种拼写，扇出去等于白花一波。
所以 A 不能简单「多发两个别名」，得先做**按命名体系去重**（中文 / 日文原名 / 罗马字 / 韩文
各取一个），否则成本翻倍收益接近零。这一步没做完之前，A 别动。
