# 观澜 待办交接（2026-08-03）

## 立刻要做的一件事

**重启容器**，新做的「🎵 找歌手」才会生效（代码已传到 NAS，md5 与本地一致：`79561a5a`）：

```bash
sudo docker restart packseed
```

我这边 `docker` 要 sudo 密码，跑不了，得你自己点。

## 这次做完的

**🎵 找歌手**（在「👤 找人」旁边）：输入歌手名 →
iTunes 拉完整年表 → 只在 5 个音乐站（OpenCD / AGSVPT / CarPT / M-Team / DICMusic）搜 →
按发行年份排序 + 经典程度排名 + 自动标「⭐推荐」→ 全选 / 只选推荐 / 一键推送到 qb（分类「音乐」）。

实测周杰伦：**7 秒，25 张有源，12 张推荐**。

两个坑已记进记忆：
1. 推 qb 必须**在容器里**发起 —— qb 免密靠 Docker 内网白名单，从宿主机连一律 403（种子本身是好的，别去查种子源）。
2. 中文专辑名**不能用字符串包含匹配** —— iTunes 给繁体（葉惠美 / 十一月的蕭邦），种子名简繁混用，
   差一个字就整张漏。改成字符集重合率 ≥0.75 后，周杰伦从 9 张涨到 25 张。

## 还没做的

- [ ] **提交这次的音乐功能**（本地 `packseed.py` 已改未提交，最后提交是 `be8d9d8`）
- [ ] 上次 403 失败的 35 张专辑 —— 重启后直接用「找歌手」推，不用再单独补
- [ ] **Emby 手动扫一次媒体库**（四大名著拆成 4 部剧、59 部剧加了 Season 目录，目录变了 Emby 要重认）
- [ ] 你自己在 Navidrome 建 `peiqi` 账号（密码我不经手）
- [ ] 音乐库还是空的；DNS 三层栈观察 1~2 周
- [ ] 可选：`cd` / `lrc` 还没加 Cloudflare Access（`emby` 是**故意**不加的）

## 参考

- 群晖现状全档：`~/Documents/claude/_档案/群晖NAS现状.md`
- 做种/媒体库两套身份的铁律：记忆 `pt-seed-vs-library-split`
